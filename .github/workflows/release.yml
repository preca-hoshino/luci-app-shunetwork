name: Release

on:
  push:
    tags:
      - 'v*'

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Build .ipk
        run: |
          chmod +x tools/ipk-build.sh
          ./tools/ipk-build.sh

      - name: Get version from tag
        id: version
        run: echo "tag=${GITHUB_REF_NAME#v}" >> "$GITHUB_OUTPUT"

      - name: Rename ipk with tag version
        run: |
          mv luci-app-shunetwork_*.ipk "luci-app-shunetwork_${{ steps.version.outputs.tag }}_all.ipk"

      - name: Create Release
        id: release
        uses: softprops/action-gh-release@v2
        with:
          name: "luci-app-shunetwork v${{ steps.version.outputs.tag }}"
          body_path: .github/release-body.md
          fail_on_unmatched_files: true
          files: |
            luci-app-shunetwork_*.ipk
